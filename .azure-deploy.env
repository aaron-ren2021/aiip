# Unified Azure Deployment Environment Variables
SUBSCRIPTION_ID="0e79512a-30a6-4276-8c8d-55bef5df741b"
LOCATION="eastus"
NAME_PREFIX="aiip"
# Core resource names (no randomness to keep idempotent). Adjust if conflict occurs.
RESOURCE_GROUP="${NAME_PREFIX}-rg"
ACR_NAME="${NAME_PREFIX}acr"              # must be globally unique (lowercase, 3-50)
AKS_CLUSTER_NAME="${NAME_PREFIX}-aks"
POSTGRES_SERVER_NAME="${NAME_PREFIX}-pg"  # flexible server global within region
POSTGRES_ADMIN_USER="admin"
POSTGRES_ADMIN_PASSWORD="Adminaaron123"   # Consider rotating later
REDIS_CACHE_NAME="${NAME_PREFIX}-redis"
SEARCH_SERVICE_NAME="${NAME_PREFIX}-search"
OPENAI_SERVICE_NAME="${NAME_PREFIX}-openai"
STORAGE_ACCOUNT_NAME="${NAME_PREFIX}stor" # must be globally unique (3-24)
STORAGE_CONTAINER_NAME="patent-documents"
KEY_VAULT_NAME="${NAME_PREFIX}-kv"
APP_INSIGHTS_NAME="${NAME_PREFIX}-insights"
LOG_ANALYTICS_NAME="${NAME_PREFIX}-logs"
ACTION_GROUP_NAME="${NAME_PREFIX}-alerts"
NOTIFICATION_EMAIL=""   # empty => skip alerts email
# OpenAI model choices (user requested gpt4.1mini; placeholder mapping)
GPT4_MODEL_NAME="gpt-4o-mini"  # approximate to gpt4.1mini request
GPT4_MODEL_VERSION="2024-05-01" # adjust to available version in subscription
GPT4_DEPLOYMENT_NAME="gpt4mini"
GPT4_CAPACITY=10
EMBEDDING_MODEL_NAME="text-embedding-3-small"
EMBEDDING_MODEL_VERSION="1"
EMBEDDING_DEPLOYMENT_NAME="embeddings-small"
EMBEDDING_CAPACITY=30
# Image tag for container deployments
IMAGE_TAG="latest"
# Optional sizing
AKS_NODE_COUNT=3
AKS_NODE_VM_SIZE="Standard_DS2_v2"
POSTGRES_SKU="B_Gen5_2"
POSTGRES_VERSION="15"
REDIS_SKU="Basic"
REDIS_VM_SIZE="C0"
SEARCH_SKU="basic"
# Feature flags
ENABLE_MONITORING_DASHBOARD=true
ENABLE_GPT4=true
